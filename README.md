<p>In order to run the application, cd task-manager/final 
    <button onclick="navigator.clipboard.writeText('cd task-manager/final')" title="Copy command">
        📋 
    </button>
    folder.
</p>
<p>Install the dependencies: with `npm install` command in terminal.</p>
<p>Enter connection string of your mongoDB database in the `.env` file.</p>
<p>Then run the application with `npm start` command in terminal.</p>

![Screenshot of app](task-manager/final/public/Screenshot%202025-12-23%20085027.png)